import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Overview/Composing Components" />

# Composing Components

With `MDX` we can include a story for `Checkbox` right in the middle of our
markdown documentation.

## Hooks

### useActiveNode

With `MDX` we can include a story for `Checkbox` right in the middle of our
markdown documentation.

### useEventListeners

With `MDX` we can include a story for `Checkbox` right in the middle of our
markdown documentation.

### usePropagateEvents

With `MDX` we can include a story for `Checkbox` right in the middle of our
markdown documentation.

### useAdjacentNodes

With `MDX` we can include a story for `Checkbox` right in the middle of our
markdown documentation.

### useActiveIndex

With `MDX` we can include a story for `Checkbox` right in the middle of our
markdown documentation.

### useActiveGridIndex

With `MDX` we can include a story for `Checkbox` right in the middle of our
markdown documentation.
